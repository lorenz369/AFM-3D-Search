# src/afm_3d_search/conf/config.yaml

defaults:
  - _self_
  - paths: default
  - models: default
  - processing: default

# A name for this specific run, used to create the output folder.
run_name: "bude_scan_v1"