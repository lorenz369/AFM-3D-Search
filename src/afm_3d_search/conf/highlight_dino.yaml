# conf/highlight_dino.yaml

defaults:
  - _self_
  - paths: default
  - models: default

paths:
  # ❗️IMPORTANT: Change this to the actual output directory of your generation run
  input_run_dir: "outputs/YYYY-MM-DD/HH-MM-SS"
  
# Parameters specific to this script
query_point_index: 75313
top_k: 7000