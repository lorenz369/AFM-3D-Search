# @package _global_
# Standard visualization settings - good for general exploration

mode: "serve"
server:
  port: 9878

rendering:
  point_size: 0.015
  create_mesh: false
  mesh_method: "poisson"
  use_voxels: false
  voxel_size: 0.05 